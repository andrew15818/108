/*given a set of circles on a plane, find a subset of non-overlapping ones*/
/*Maybe first sort the coords according to x val, then check overlapping counts of every subhalf?*/
#include<cstdio>
using namespace std;
struct Circle{
	long long int x, y, r;
}
int main(){
	long int n;
	cin>>n;
	Circle circles[n];
	for(long int i =0;i<n;i++){
		
	}
	return 0;
}
